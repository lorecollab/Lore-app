# LORÉ Test Credentials

## Test user (pre-seeded via signup during smoke test)
- Email: `test@lore.app`
- Password: `testpass123`
- Name: `Test Sage`

You can create additional users via `POST /api/auth/signup` with `{email, password, name}`.
